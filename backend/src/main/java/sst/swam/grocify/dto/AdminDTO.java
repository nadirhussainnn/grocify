package sst.swam.grocify.dto;

public class AdminDTO extends UserDTO {
	
	// We do not associate some data to admin, so we keep it empty. 
	// For exp: Customer has orders. Admin does not have something like that
}
